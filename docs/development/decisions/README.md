# 架构决策

本目录保存影响多个模块、具有长期约束或存在重要替代方案的决策记录。

当前已锁定决策仍集中在 [`../DESIGN.md`](../DESIGN.md) 的“已锁定的决策”章节。为避免一次性搬迁造成
引用漂移，本轮只建立索引；后续修改某项决策时，再把它提取为独立 ADR，并从 `DESIGN.md` 链接回来。

新的 ADR 使用 `ADR-NNN-主题.md`，至少包含：状态、日期、上下文、决策、替代方案、后果、验证方式和
被替代关系。ADR 记录理由；当前可执行契约仍写回 `ARCHITECTURE.md` 或 `DESIGN.md`。
